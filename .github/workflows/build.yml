# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the master branch
on:
  pull_request:
  push:
    branches: main

jobs:
  build-esp32s2-basic:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: 'true'
    - name: Set up Python
      uses: actions/setup-python@v1
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install platformio

    
    - name: Install platformIO libraries
      working-directory: ./ESP32S2_Basic
      run: pio lib install

    - name: Run PlatformIO
      working-directory: ./ESP32S2_Basic
      run: platformio run
    
    - name: Upload Artifacts
      uses: actions/upload-artifact@v3
      with:
        # Artifact name
        name: build-artifacts
        path: ./ESP32S2_Basic/.pio/build/**
        if-no-files-found: error
        retention-days: 90
  
  build-esp32s3-basic:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: 'true'
    - name: Set up Python
      uses: actions/setup-python@v1
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install platformio

    
    - name: Install platformIO libraries
      working-directory: ./ESP32S3_Basic
      run: pio lib install

    - name: Run PlatformIO
      working-directory: ./ESP32S3_Basic
      run: platformio run
    
    - name: Upload Artifacts
      uses: actions/upload-artifact@v3
      with:
        # Artifact name
        name: build-artifacts
        path: ./ESP32S3_Basic/.pio/build/**
        if-no-files-found: error
        retention-days: 90

  build-esp32s2-wifimamager:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: 'true'
    - name: Set up Python
      uses: actions/setup-python@v1
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install platformio

    
    - name: Install platformIO libraries
      working-directory: ./ESP32S2_WiFiManager
      run: pio lib install

    - name: Run PlatformIO
      working-directory: ./ESP32S2_WiFiManager
      run: platformio run
    
    - name: Upload Artifacts
      uses: actions/upload-artifact@v3
      with:
        # Artifact name
        name: build-artifacts
        path: ./ESP32S2_WiFiManager/.pio/build/**
        if-no-files-found: error
        retention-days: 90
    